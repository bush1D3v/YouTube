// código .ts -> Parser -> Binder -> Checker -> Emitter - JavaScript

// Parsing
// Lê o código e gera o AST.

// Binding
// Cria a tabela de símbolos (quem é quem no código)

// Checking
// Verifica se os tipos batem, aplica regras do TS (strict, noImplicityAny, etc)

// Emitting
// Emitir código JavaScript (e .d.ts se configurado)